<div id="login" v-cloak>
  <div class="container">Accede a tu cuenta</h1>
    <div class="login-form-container">
      <hr/>
      <ajax-form action="login" :syncing.sync="syncing" :cloud-error.sync="cloudError" :form-data="formData" :form-rules="formRules" :form-errors.sync="formErrors" @submitted="submittedForm()">
        <div class="form-group">
          <input type="email" class="form-control" placeholder="Email " :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" autocomplete="email" focus-first>
          <div class="invalid-feedback" v-if="formErrors.emailAddress">Ingresa un email válido.</div>
        </div>
        <div class="form-group">
          <input type="password" class="form-control" placeholder="Contraseña" :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password" autocomplete="current-password">
         <div class="invalid-feedback" v-if="formErrors.password">Ingrese su contraseña porfavor.</div>
        </div>
        <div class="form-group form-check">
          <input class="form-check-input" type="checkbox" id="remember" name="rememberMe" v-model="formData.rememberMe"/>
          <label class="form-check-label" for="remember">Recuerdame</label>
        </div>
        <p class="text-danger" v-if="cloudError==='badCombo'"><small>Las credenciales ingresadas son erróneas. Por favor verifica email o contraseña e intenta de nuevo.</small></p>
        <p class="text-danger" v-else-if="cloudError"><small>Un error ha ocurrido mientras se procesaba tu solicitud. Por favor verifica tu información en intenta de nuevo <a href="/contact">contacta a soporte</a> si el error persiste.</small></p>
        <div class="form-group">
          <ajax-button :syncing="syncing" class="btn-dark btn-lg btn-block">Sign in</ajax-button>
        </div>
      </ajax-form>
      <p class="text-center"><a href="/password/forgot">Olvidaste tu contraseña?</a></p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
